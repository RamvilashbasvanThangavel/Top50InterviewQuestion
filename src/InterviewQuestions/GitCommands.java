package InterviewQuestions;

public class GitCommands {
	public static void main(String[] args) {

		System.out.println("Push commands " + "\n");
		// git config --global user.name "Ramvilashbasvan"
		// git config --global user.email "rvbramvilash@gmail.com"
		// cd (worksapce)
		// git init
		// git add .
		// git remote add origin (git url)
		// git commit -m "name any for your identify"
		// git push -u origin master

		System.out.println("Pull commands " + "\n");
		// cd (workspace)
		// git init
		// git pull (git url)

		System.out.println("clone commands " + "\n");
		// cd (workspace)
		// git clone (git url)

		System.out.println("Add branch commands " + "\n");
		// cd (worksapce)
		// git add .
		// git remote add origin (git url)
		// git checkout -b "Task name"
		// git commit -m "name any for your identify"
		// git push -u origin Task name
	}

}
